// Stub implementation for web APIs on non-web platforms.

/// Stub class for HTMLCanvasElement.
class HTMLCanvasElement {
  // Add properties/methods if they are accessed by the shared code,
  // otherwise, an empty class is sufficient for type checking.
  HTMLCanvasElement() {
    throw UnimplementedError('HTMLCanvasElement is only available on web.');
  }
}

// Add other web types here if needed by the shared code.
